# Copy this file to config.yaml and replace the placeholder values.
# config.yaml is ignored by Git. Do not put AWS credentials in it; authenticate
# through your AWS CLI profile, IAM Identity Center, or environment credentials.

project:
  name: k8s-learning

k8s:
  version: 1.36.0

aws:
  region: ap-south-1

  state_bucket:
    name: replace-with-your-private-state-bucket
    key_prefix: k8s-on-aws

  compute:
    control_plane:
      instance_type: t3a.small
    worker:
      instance_type: t3a.small
    root_volume_size: 20

  networking:
    vpc:
      private_subnet_values:
        - 10.0.48.0/20
        - 10.0.64.0/20
        - 10.0.80.0/20
      public_subnet_values:
        - 10.0.0.0/20
        - 10.0.16.0/20
        - 10.0.32.0/20
  
  dns.private_domain_name: k8s.internal

